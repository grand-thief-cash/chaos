# config/app.yaml
logging:
  level: "INFO"
  log_dir: "logs"
  backup_count: 30
  format: "[%(asctime)s][%(filename)s:%(lineno)d][%(levelname)s][%(thread)d][%(traceid)s][%(execution_time).2f s][%(message)s]"

# 未来可扩展其他组件配置
mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "mydb"