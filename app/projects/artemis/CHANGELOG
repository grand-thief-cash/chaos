# VERSION
- v0.36.3

# Changelog
- v0.36.3
    - **Baostock adjust factor download task**
        - Added `STOCK_ZH_A_BS_ADJUST_FACTOR_PARENT` / `STOCK_ZH_A_BS_ADJUST_FACTOR_CHILD` to download A-share adjust factor data via `baostock.query_adjust_factor()`.
        - Implemented plan/child orchestration that loads the security universe from PhoenixA and expands one child task per symbol.
        - Added PhoenixA client support for the new standalone adjust factor ingestion API.
        - Added task config and a new CronJob task export file for scheduled imports.
- v0.36.2
    - **BI review iteration (time axis + DuPont tree + trend horizon)**
        - `bi_service.py` trend sections now output periods in chronological order (oldest -> newest) for chart rendering.
        - Increased BI statement query window (`page_size`) and default trend horizon to support longer-term views (default 12 periods).
        - Expanded DuPont response tree from shallow 1-level drivers to recursive nodes including formula operands (net profit/revenue/avg assets/avg equity).
        - Updated BI design doc `docs/2026-05-19 BI_SERVICE_POSITIONING_AND_FINANCIAL_DASHBOARD_DESIGN.md` with review decisions and contract constraints.
        - Refreshed BI service tests to validate chronological periods and deeper DuPont decomposition nodes.
- v0.36.1
    - Fixed bi_service.py for missing symbols.
    - Filtered for merged financial statements only PhoenixA statement_code uses numeric codes: "1".
    - PhonexA_client added `statement_code` in query_financial_statements parameters.
- v0.36.0
    - **BI MVP backend foundation**
        - Added new `/bi` route group in Artemis for structured financial BI read models
        - New endpoints:
            - `GET /bi/financial/company/{symbol}/dashboard`
            - `GET /bi/financial/company/{symbol}/dupont`
            - `GET /bi/financial/company/{symbol}/quality`
            - `GET /bi/meta/metrics`
        - Added BI Pydantic models under `artemis/models/bi.py`
        - Added BI service layer under `artemis/services/bi_service.py`
        - Added BI metric contract definitions under `artemis/engines/bi_engine/contracts/metric_definitions.py`
        - Implemented PhoenixA-backed Phase 1 financial metric computation for dashboard / dupont / quality panels
        - Scope explicitly kept BI-only: no Atlas / narrative / LLM integration in this MVP implementation
    - **BI documentation refinement**
        - Updated `docs/2026-05-19 BI_SERVICE_POSITIONING_AND_FINANCIAL_DASHBOARD_DESIGN.md`
        - Added MVP metric matrix table (`page -> module -> metric -> PhoenixA field -> phase`)
        - Added detailed BI read-model schemas for Artemis API responses
        - Added detailed Cthulhu route/page inventory and explicit BI-only scope statement
    - **Tests**
        - Added `tests/test_bi_service.py` for BI metric calculation / dupont / quality contract coverage
        - Added `tests/test_bi_routes.py` for BI route smoke coverage
        - Verified BI backend tests pass: `3 passed`
- v0.36.1
    - **Factor catalog merge logic fix**
        - Fixed factor catalog loading to merge definitions from multiple YAML files instead of overriding
        - `governance_seed.yaml` now supplements (not replaces) `fundamental_core.yaml` definitions
        - Resolved issue where `asset_turnover`, `roic`, and other factors showed "0 field(s)" Required Fields and "No provenance yet"
        - All factors now correctly retain `source_fields`, `availability`, and `provenance` information from core definitions
        - `deploy_artemis.py` included supporting upload factor_catalog config.
- v0.36.0
    - **Factor Engine × PhoenixA follow-up review fixes**
        - Fixed industry-map cache pollution: subset `get_industry_map(..., symbols=[...])` requests no longer poison the full-market cache; full-map cache and per-symbol industry-context cache are now separated
        - Fixed `run_incremental()` tail logging crash caused by stale `skipped_symbols` reference; completion logs now keep only meaningful `failed_symbols` diagnostics
        - Fixed `get_active_symbols(as_of_date)` survivorship issue: PhoenixA provider now preserves `list_date` / `delist_date` from `security_registry` and applies as-of filtering during historical rebuilds
        - Improved `/factors/availability` runtime semantics: capabilities-unreachable / empty / untrusted states now surface as `unknown` instead of being flattened into `missing`
        - Availability analysis now validates both required sources and required fields; source-present but field-incomplete factors are downgraded to `partial` / `missing` instead of false `available`
        - Added source-scoped factor-engine runtime isolation across `/factors/availability`, `/factors/compute/*`, `/factors/snapshot`, and `/factors/rank`, so different Workbench sources no longer share in-memory provider/store/pipeline state
        - PhoenixA taxonomy `derived_flags.financial_sector` is now used as the fallback signal for financial-sector exclusion when statement-level `comp_type_code` is unavailable
    - **Regression coverage refresh**
        - Added focused tests for unreachable capabilities → `unknown`, missing required-field detection, as-of active-symbol filtering, and industry subset/full cache separation
- v0.35.0
    - **Factor Engine Cache & Performance Improvements**
        - Added TTL-based cache expiration with `CacheEntry` dataclass (`timestamp`, `ttl_seconds`, `is_expired()`)
        - Defined cache TTL constants: `CACHE_TTL_SYMBOLS` (1h), `CACHE_TTL_INDUSTRY_MAP` (24h), `CACHE_TTL_FINANCIAL` (5m), `CACHE_TTL_MARKET_DATA` (5m), `CACHE_TTL_CORP_ACTION` (1h)
        - Added concurrent batch query support for industry mapping with `get_industry_map_batch()` using ThreadPoolExecutor (10 workers, batch size 50)
        - Added `symbols` parameter to `get_industry_map()` for incremental queries (only fetch needed symbols)
        - All cache lookups now check `is_expired()` before returning cached values
        - Fixed `get_industry_context()` to correctly extract `CacheEntry.value` from cache
    - **Pipeline Error Handling & Logging**
        - Enhanced `run_full()` with granular try-except blocks at symbol, group, and pipeline levels
        - Added `skipped_symbols` and `failed_symbols` tracking for better pipeline diagnostics
        - Added completion summary logging with total_symbols, skipped_count, failed_count
        - Enhanced `run_incremental()` with batch industry queries (`use_batch=True`) and error handling
    - **Factor Catalog Governance**
        - Added comprehensive PEG factor definition to `governance_seed.yaml` with business logic documentation
        - Specified growth definition (净利润单季度同比增长率), validity conditions (增长率 > 0 且 PE_TTM > 0), corner cases (扭亏为盈/盈转亏), and alternatives
        - Verified dividend_yield, dps, and valuation factors match catalog definitions
        - Verified EV/EBITDA fallback logic matches catalog (op_ttm + fin_ttm)
    - **Tests**: 82/83 PASSED (1 unrelated failure in TestMarketAdjustPolicy)
- v0.34.0
    - Taxonomy matching now directly consumes PhoenixA `canonical_*` + `derived_flags` industry contract, and Artemis-side industry-prefix fallback has been removed before launch
    - Factor-engine market-data retrieval now uses shared `ADJUST_*` constants and exposes explicit bars `adjust` control through the PhoenixA provider (default remains `ADJUST_NONE`)
    - Factor-engine bars adjust is now governed only by factor catalog `market_adjust_policy`; runtime config defaults/overrides were removed to keep adjust tied to factor definition
    - Cleaned factor catalog governance model: `phoenix_queries` no longer repeats `adjust=nf`, and bars adjust semantics now live only in `market_adjust_policy`
- v0.33.0
    - **Factor Engine × PhoenixA contract review and fixes**
        - Normalized factor-engine `as_of_date`, `ann_date`, and `reporting_period` handling across `YYYYMMDD` and `YYYY-MM-DD`
        - Factor formulas are now aligned directly to PhoenixA canonical field names (`INCOME_TAX`, `TOTAL_PROFIT`, `NET_CASH_FLOW_OPERA_ACT`) instead of relying on Artemis-side aliases
        - Fixed PhoenixA bars integration bug: factor provider now requests raw `trade_date`/`symbol` fields (`normalize_for_cache=False`)
        - Fixed valuation/per-share sourcing: `market_cap` now computed from `close × TOT_SHARE`, `dps`/`dividend_yield` now use `corporate-action/dividend`
        - Implemented `PEG` calculation in `valuation.py` (using current net-profit YoY growth definition)
        - `EV/EBITDA` now prefers PhoenixA `EBITDA` field when available
        - `exclude_financial=True` factor policies now actually suppress unsupported factors for banks/insurers/brokers
        - Factor ranking now honors `higher_is_better` metadata instead of always sorting descending
    - **Factor registry metadata enrichment**
        - `registry.py` now exposes `data_sources`, `ttm_required`, and `min_history_quarters`
        - Added `get_factor_meta()` for policy-aware service logic
        - Added YAML-backed factor catalog seed under `config/factor_catalog/` for governance-critical factors
        - `/factors/meta` now exposes management description, LaTeX formula, PhoenixA lineage, dividend/market-cap governance, and financial policy metadata
        - Expanded factor catalog coverage to all 39 registered factors via `config/factor_catalog/factors/fundamental_core.yaml`
        - Added static `availability.expected` / `availability.requirements` metadata so factor management can distinguish ready vs conditional factors before runtime computation
        - Added factor-level `required_fields`, `required_data_sources`, and structured `provenance` so the catalog can describe exact PhoenixA fields and query lineage per factor
    - **Snapshot runtime management metadata**
        - Full and incremental factor snapshots now persist `reporting_period`, `latest_ann_date`, `freshness`, `company_kind`, and per-factor `missing_reasons`
        - Missing reasons currently classify common failure modes such as financial-sector exclusion, missing market data, missing statement sources, and insufficient history quarters
        - Missing-reason classification is now more granular, including required-source gaps, required-field gaps, market-data frame gaps, and missing dividend enrichment
    - **PhoenixA client enhancements for factor engine**
        - `query_corporate_actions()` now supports `symbols`, `report_period`, `ann_date_before`, `progress_code`, and `fields`
        - `query_financial_statements()` now mirrors the controller contract more completely (`symbols`, `market`, `reporting_period`, `fields`, `page`, `page_size`)
        - Added `get_catalog_capabilities()` to consume PhoenixA `/api/v2/catalog/capabilities` for factor availability analysis
    - **Availability endpoint**
        - New `/factors/availability` API combines factor catalog metadata with PhoenixA catalog capabilities
        - Returns factor-level provenance, required fields, required data sources, source availability, and per-factor availability status (`available` / `partial` / `missing` / `unknown`)
    - **Tests**
        - Expanded `test_factor_engine.py` with canonical-field and factor-catalog governance coverage
        - Expanded `test_phoenixa_factor_provider.py` with canonical taxonomy / derived-flags coverage and configurable market-adjust assertions
        - Added regression coverage for snapshot freshness / missing-reason metadata and store persistence
        - Added dedicated availability service/route coverage (`test_factor_availability.py`)
        - Verified factor-engine/provider/availability suite passes after this round of updates: 103 tests
    - **Review & design docs**
        - New doc: `docs/2026-05-14 [REVIEW] FACTOR_ENGINE_PHOENIXA_INTEGRATION_AND_FACTOR_MANAGEMENT.md`
        - Documents PhoenixA contract audit, factor computability matrix, canonical field naming policy, standardized taxonomy recommendation, hybrid factor-catalog design, and Cthulhu presentation proposal
        - Updated `docs/2026-05-08 [IMPLEMENTING] FEATURE_FUNDAMENTAL_FACTOR_ENGINE.md` with factor catalog / dividend / market-cap governance addendum
        - Updated PhoenixA API docs and internal catalog metadata to match the actual financial controller contract
- v0.32.0
    - **Fixed Baostock parent tasks parameter validation error**
        - Removed `parameter_check()` from `STOCK_ZH_A_BS_DIVIDEND_PARENT` and `STOCK_ZH_A_BS_BALANCE_PARENT`
        - These tasks were failing with "Missing required param: start_date" because `parameter_check` runs BEFORE config merging
        - Config parameters (from task.yaml) are now only validated in `before_execute()` where `ctx.params` is available
    - **Telemetry component enhancements**
        - Added `ExporterFile` type to explicitly distinguish file output from stdout
        - Fixed `sample_ratio: 0` behavior — now correctly disables all telemetry sampling (uses `NeverSample` sampler)
        - Fixed `exporter: stdout` writing to file — now only writes to file when `exporter: file` or `stdout_file` is explicitly set
        - File rotation now uses configurable defaults: 100MB max size, 7 days retention, 5 backups
        - Telemetry config updated to use `exporter: file` for file-based output (new recommended config)
    - **Factor availability checker script**
        - New script: `scripts/check_factor_availability.py`
        - Queries PhoenixA `/api/v2/catalog/capabilities` endpoint
        - Compares factor requirements with available data sources
        - Reports which factors are fully available, partially available, or missing data
        - Supports text and JSON output formats
    - **Cthulhu Workbench Factor Engine documentation**
        - New doc: `docs/2026-05-11 CTHULHU_WORKBENCH_FACTOR_ENGINE_GUIDE.md`
        - Comprehensive guide for Factor Registry, Compute, Snapshot, and Ranking features
        - Explains "Mkt Data" column meaning: No = financial data only, Yes = requires market data (price/volume)
        - Documents all API endpoints and their parameters/responses
        - Includes best practices and troubleshooting guide
    - **Go infra telemetry component documentation update**
        - Documented `exporter: file` as the recommended way to write to file with rotation
        - `exporter: stdout` now always writes to actual stdout (no file redirection)
- v0.31.0
    - **Baostock quarterly solvency data download** (query_balance_data)
        - New tasks: `STOCK_ZH_A_BS_BALANCE_PARENT` / `STOCK_ZH_A_BS_BALANCE_CHILD` — plan/child mode, per-symbol per-(year,quarter)
        - Parent: fetches symbol list from PhoenixA, expands start_date/end_date into (year, quarter) matrix
        - Child: calls `bs.query_balance_data(code, year, quarter)`, converts to financial_statement format (statement_type="bs_balance")
        - Data fields: currentRatio, quickRatio, cashRatio, YOYLiability, liabilityToAsset, assetToEquity
        - Reuses `financial_statement` table with source="baostock"
    - **Baostock ex-rights/dividends data download** (query_dividend_data)
        - New tasks: `STOCK_ZH_A_BS_DIVIDEND_PARENT` / `STOCK_ZH_A_BS_DIVIDEND_CHILD` — plan/child mode, per-symbol per-year
        - Parent: expands start_date/end_date into year list, supports year_type param ("report"/"operate")
        - Child: calls `bs.query_dividend_data(code, year, yearType)`, converts to corporate_action format (action_type="bs_dividend")
        - Data fields: dividCashPsBeforeTax, dividCashPsAfterTax, dividStocksPs, dividReserveToStockPs, key dates
        - Auto-derives progress_code: implemented/announced/planned
        - Post-process dedup by (source, symbol, market, action_type, ann_date)
        - Reuses `corporate_action` table with source="baostock"
    - **New utility functions** (download/zh/utils.py)
        - `date_range_to_year_quarters()`: YYYY-MM-DD date range → (year, quarter) list
        - `symbol_exchange_to_bs_code()`: (symbol, exchange) → baostock code format (e.g. "sh.600000")
    - **CronJob config**: `tasks_export_2026-05-11.json`
        - `stock_zh_a_bs_balance`: weekly SAT 09:30, ASYNC, 60min timeout
        - `stock_zh_a_bs_dividend`: weekly SAT 11:00, ASYNC, 60min timeout
    - **Unit tests**: `test_baostock_tasks.py` — 30 tests covering utils, balance child post_process, dividend child post_process
- v0.30.0
    - Implemented phoenixA provider.
    - Upgraded akshare version.
    - Replaced phoenixA mock provider.
- v0.29.0
    - **Code list source migration: SDK → PhoenixA**
        - `BaseFinancialStatementTask` and `BaseCorporateActionTask` no longer call `self._base_data.get_hist_code_list()` for full symbol lists
        - New `utils.get_code_list_from_phoenixa(ctx)` fetches securities from PhoenixA `GET /api/v2/securities` and converts to SDK format (`["000001.SZ", "600519.SH"]`)
        - Removed `self._base_data = ad.BaseData()` from both base classes (no longer needed)
        - Better error messages: `"empty code_list for ... (mode=...; check PhoenixA /api/v2/securities)"` instead of generic "no valid symbols"
        - Guard against `NoopDeptServiceClient` — logs warning instead of crashing with `AttributeError`
    - **OTEL trace_id propagation to async task threads**
        - `TaskEngine.run()` now captures OpenTelemetry context before spawning `threading.Thread` and restores it in the worker thread
        - Task logs in async mode now include `trace_id` and `span_id` — previously only HTTP access logs had these fields
        - Fixed critical memory leak: `_unregister_task` was never called when OTEL was installed (every task leaked an entry in `_active_tasks`)
    - **Financial/Corporate Action upsert dedup**
        - `post_process()` in both base classes now deduplicates records by unique key before sinking
        - Fixes `ON CONFLICT DO UPDATE command cannot affect row a second time` error when SDK returns duplicate rows for the same symbol+period
    - **Log rotation fix**
        - `_apply_config` now reads `rotate_config.max_age` (e.g. `72h`) to compute `backupCount` — was hardcoded to 7
        - Startup cleanup: old rotated log files beyond `max_age` are deleted immediately on startup, not just at midnight rotation
- v0.28.0
    - **Symbol format & date normalization for financial/corporate_action data**
        - `split_market_code()`: `"000001.SZ"` → `symbol="000001", market="zh_a"` — all tables now store pure codes, matching bars/taxonomy/security_registry convention
        - `normalize_date_yyyymmdd()`: `"20260425"` → `"2026-04-25"` — all date fields normalized to ISO YYYY-MM-DD
        - `BaseFinancialStatementTask.post_process()`: applies both transformations
        - `BaseCorporateActionTask.post_process()`: applies both transformations
        - `task.yaml`: `STOCK_ZH_A_DIVIDEND` and `STOCK_ZH_A_RIGHT_ISSUE` symbols now use pure code format (e.g., `["000001"]`) with separate `exchange` field, consistent with `STOCK_ZH_A_BALANCE_SHEET`
- v0.27.0
    - **Industry data fixes — trade_date format + taxonomy_security_map sync + naming unification + parent_code bug fix**
        - Fixed `STOCK_ZH_A_INDUSTRY_WEIGHT_SWHY_CHILD` `trade_date` format: raw SDK value (YYYYMMDD, e.g. `20260507`) now normalized to `YYYY-MM-DD` (e.g. `2026-05-07`) before sinking to PhoenixA, consistent with `industry_daily` task
        - `STOCK_ZH_A_INDUSTRY_CONSTITUENT_SWHY` sink: after upserting constituents, now calls `sync_mappings_from_constituents()` to auto-populate `taxonomy_security_map` (derives category_code → symbol mappings from constituent + category data via PhoenixA JOIN endpoint)
        - `PhoenixAClient`: new `sync_mappings_from_constituents()` method calling `POST /api/v2/taxonomy/{source}/{taxonomy}/{market}/mapping/sync_from_constituents`
        - **Naming: `symbols` → `index_codes` for industry tasks** — resolves ambiguity where `symbols` meant stock codes in stock tasks but index codes in industry tasks; `_resolve_index_codes()` and all 3 industry task `parameter_check` methods now read `index_codes` parameter
        - Fixed `config/task.yaml` defaults for `STOCK_ZH_A_INDUSTRY_WEIGHT_SWHY` and `STOCK_ZH_A_INDUSTRY_DAILY_SWHY`: removed hardcoded test values (`start_date: "2025-05-01"`, `symbols: ["851426.SI"]`), now correctly set to `start_date: null`, `end_date: null` (matching documentation)
        - **Bug fix: `STOCK_ZH_A_MKT_CATEGORY_SWHY` parent_code always NULL** — replaced code-prefix matching (assumed fixed 2/4/6-digit INDUSTRY_CODE) with name-based hierarchy matching using LEVEL1_NAME/LEVEL2_NAME fields; now correctly resolves parent for level-2 and level-3 entries regardless of INDUSTRY_CODE format
        - `STOCK_ZH_A_INDUSTRY_WEIGHT_SWHY_CHILD` post_process: added diagnostic logging showing skipped code, data type, and processed count for debugging empty SDK results
        - Added dedup debug logging to `STOCK_ZH_A_INDUSTRY_CONSTITUENT_SWHY` and `STOCK_ZH_A_INDUSTRY_DAILY_SWHY_CHILD` — logs `duplicates_dropped` count and `kept` count when dedup occurs

- v0.26.0
    - **Factor Engine** — Fundamental factor computation engine (`artemis/engines/factor_engine/`)
        - 7 factor groups with 39 factors implemented and self-registered:
            - **Profitability** (6): ROE, ROA, Gross Margin, Operating Margin, Net Margin, ROIC
            - **Growth** (5): Revenue/NI Growth YoY (single-quarter based), Revenue/NI CAGR 3Y, OCF Growth
            - **Quality** (5): Accrual Ratio, Cash Conversion, FCF Quality, Earnings Stability, Goodwill Ratio
            - **Solvency** (6): Debt Ratio, Current/Quick Ratio, Interest Coverage, Net Debt/EBITDA, Cash/ST Debt
            - **Valuation** (7): PE_TTM, PB, PS_TTM, PEG, EV/EBITDA, PCF, Dividend Yield
            - **Efficiency** (5): Asset/Inventory/Receivable Turnover, Cash Cycle, Capex/Revenue
            - **Per Share** (5): EPS_TTM, BPS, CFPS, FCF Per Share, DPS
        - `ttm.py`: Chinese cumulative statement to TTM conversion + single-quarter derivation with Q1-Q4 boundary handling
        - `point_in_time.py`: PIT time alignment via `ann_date` filter, prevents look-ahead bias, supports report revision dedup
        - `normalizer.py`: MAD winsorization (MAD=0 fallback), industry Z-Score (std=0 fallback), incremental normalization, market-cap neutralization
        - `pipeline.py`: Full `run_full()` + incremental `run_incremental()` pipeline, Protocol-based data provider (swappable)
        - `storage/factor_store.py`: In-memory MVP storage with PhoenixA-compatible interface for future migration
        - `registry.py`: Global factor registry with auto-registration + metadata query
        - Financial sector exclusion flag (`exclude_financial`), data freshness scoring (`FactorFreshness`)
    - **Market Regime Engine** — Continuous state-space market regime estimator (`artemis/engines/regime_engine/`)
        - State-Space design: 6-dimensional continuous state vector + 2 transition signals
            - `trend_strength`, `risk_appetite`, `volatility_stress`, `market_breadth`, `liquidity`, `sector_concentration`
            - `breadth_momentum` (leading indicator), `vol_acceleration` (leading indicator)
        - 6 MVP feature computers: Trend, Breadth, Volatility, Liquidity, Style, Industry
        - `estimator.py`: Continuous state estimator with sigmoid mapping + EMA smoothing (asymmetric: vol_stress rises instantly, decays smoothly)
        - `allocator.py`: Continuous strategy weight mapping (no if-else) + position limit + factor weight adjustment
        - `pipeline.py`: Full run/backfill pipeline with Protocol-based data provider
        - `models.py`: `derive_labels()` derives discrete labels from continuous scores (for logging/frontend only, strategy engine does NOT consume labels)
        - `storage/regime_store.py`: In-memory MVP storage
    - **Services & API**
        - `services/factor_service.py`: Factor computation service + mock data provider
        - `services/regime_service.py`: Regime service + mock data provider
        - `api/http_gateway/factor_routes.py`: `/factors/compute/full`, `/factors/compute/incremental`, `/factors/snapshot`, `/factors/rank`, `/factors/meta`
        - `api/http_gateway/regime_routes.py`: `/regime/compute`, `/regime/backfill`, `/regime/current`, `/regime/history`, `/regime/features`
        - Both routers registered in main FastAPI app
    - **Bug fixes during review**
        - Fixed `get_quarter()`/`get_year()` crashing on empty/short/garbage input strings (now returns 0)
        - Fixed `_val()` silent mismatch when `reporting_period` column is int but period param is string (now casts to str)
        - Fixed `_val()` KeyError when requested field does not exist in DataFrame (now returns None)
        - Fixed `_sigmoid_clip()` OverflowError for extreme input values (now clamps exp input)
        - Fixed `zscore_by_industry()` silently dropping symbols not in `industry_map` (now assigns sentinel group)
    - **Tests**
        - `test_factor_engine.py`: 58 tests covering TTM, single-quarter, PIT, safe_div, normalizer MAD/Z-Score/incremental, registry, avg_balance
        - `test_regime_engine.py`: 33 tests covering clip, sigmoid, label derivation, estimator smoothing, allocator weights/position, serialization
    - Design docs:
        - `docs/2026-05-08 [PLANNING] FEATURE_FUNDAMENTAL_FACTOR_ENGINE.md` (rev.2)
        - `docs/2026-05-08 [PLANNING] FEATURE_MARKET_REGIME_ENGINE.md` (rev.2 — continuous state-space refactor)
- v0.25.1
    - **Config hardening — task.yaml naming fix + Mairui key externalization + task rename**
        - Fixed `timeframe → period` naming inconsistency in `config/task.yaml` for `BACKTRADER_CAMPAIGN` and `BACKTRADER_RUN` variants (was using `timeframe`, code `parameter_check` requires `period`)
        - Extracted hardcoded Mairui API license from `stock_zh_a_mkt_category_mairui.py` into `config.yaml` under `sdk.mairui.license`; task now reads from config with clear error if missing
        - Renamed `STOCK_ZH_A_MKT_CATEGORY` → `STOCK_ZH_A_MKT_CATEGORY_MAIRUI` (task code, class `StockZHAMktCategoryMairui`, file `stock_zh_a_mkt_category_mairui.py`) to eliminate ambiguity with `STOCK_ZH_A_MKT_CATEGORY_SWHY`
        - Fixed `get_symbols_from_params()` silently falling back to full download when `symbols` is provided but `exchange` is missing — now raises `ValueError` so the task fails immediately with a clear error instead of hanging on unintended full download
    - **Industry WEIGHT/DAILY orchestrator refactor** — split into OrchestratorUnit + WorkerUnit per index code
        - `STOCK_ZH_A_INDUSTRY_WEIGHT_SWHY`: now OrchestratorUnit, resolves index codes then plans one `_CHILD` per index
        - `STOCK_ZH_A_INDUSTRY_DAILY_SWHY`: same refactor
        - New child task codes: `STOCK_ZH_A_INDUSTRY_WEIGHT_SWHY_CHILD`, `STOCK_ZH_A_INDUSTRY_DAILY_SWHY_CHILD`
        - Each child downloads + processes + sinks a single industry index, enabling fine-grained retry, progress tracking, and timeout isolation
        - `STOCK_ZH_A_INDUSTRY_CONSTITUENT_SWHY` unchanged (static data, small payload)
        - Cronjob-facing task code names unchanged — no cronjob config updates needed
        - Fixed industry tasks incorrectly using `get_symbols_from_params()` (stock pattern requiring `symbols`+`exchange`) — industry `symbols` are SDK-format index codes (e.g. `["851426.SI"]`), no `exchange` needed; extracted shared `_resolve_index_codes()` helper
        - Fixed child tasks losing `trace_id` in logs — OpenTelemetry context was not propagated to ThreadPoolExecutor worker threads; added `_run_with_context()` wrapper that captures and attaches the parent's OTel context
        - Fixed `STOCK_ZH_A_INDUSTRY_DAILY_SWHY_CHILD` sink failure: SDK returns `nan`/`inf` float values which `json.dumps()` cannot serialize — added `_safe_float()` helper that replaces non-finite values with `0.0`; applied to both DAILY and WEIGHT child tasks
        - Fixed `STOCK_ZH_A_INDUSTRY_DAILY_SWHY_CHILD` PostgreSQL upsert error: `ON CONFLICT DO UPDATE command cannot affect row a second time` — added deduplication by `(index_code, trade_date)` in `post_process` to prevent duplicate unique-key rows in a single batch
- v0.25.0
    - **Incremental Download & Parameter Unification** — all download tasks now support per-symbol, per-date-range incremental pulls
        - **Shared utilities** (`download/zh/utils.py`):
            - New `get_symbols_from_params()`: reads `symbols` (pure codes) + `exchange` from ctx.params, combines at SDK boundary (e.g. `["000001"] + "SZ"` → `["000001.SZ"]`)
            - New `get_sdk_date_kwargs()`: maps unified `start_date`/`end_date` (YYYY-MM-DD str) → SDK `begin_date`/`end_date` (int)
            - Eliminates duplicate `_parse_date_to_int()` from industry weight/daily tasks
        - **Financial statement tasks** (base + 5 subclasses):
            - `BaseFinancialStatementTask.execute()` refactored: supports explicit `symbols`+`exchange` (incremental) or full SDK code list (default)
            - `_sdk_call(**sdk_date_kwargs)`: all 5 subclasses pass through date params to SDK
            - SDK support confirmed per `AmazingData_development_guide.md` V1.0.24: all 5 APIs accept `begin_date`/`end_date` (报告期)
        - **Corporate action tasks** (base + 2 subclasses):
            - Same refactoring as financial statements
            - SDK support: `get_dividend`/`get_right_issue` accept `begin_date`/`end_date` (公告日期)
        - **Industry tasks** (3 tasks):
            - `StockZHAIndustryWeightSWHY` / `StockZHAIndustryDailySWHY`: migrated to shared `get_symbols_from_params()` + `get_sdk_date_kwargs()`
            - `StockZHAIndustryConstituentSWHY`: uses `get_symbols_from_params()` only (SDK does NOT support date params)
        - **Parameter conventions unified across project**:
            - Dates: always `"YYYY-MM-DD"` string in params, converted to int only at SDK call boundary
            - Symbols: `symbols: ["000001"]` + `exchange: "SZ"` (separate fields, matching `security_registry` storage)
            - No auto-resolution or format guessing
        - **task.yaml**: added config entries for all 7 financial/corporate action tasks with `start_date`/`end_date` defaults
        - New doc: `2026-05-07 INCREMENTAL_DOWNLOAD_AND_SYMBOL_RESOLUTION.md`
- v0.24.0
    - **Taxonomy V2** — unified source/taxonomy/market for industry data pipeline
        - New `Taxonomy` enum in `consts/data_source.py` (swhy/citic/gics/concept/region/mairui)
        - `PhoenixAClient`: all taxonomy/industry methods now accept `taxonomy` and `market` params; API paths updated to `/api/v2/taxonomy/{source}/{taxonomy}/{market}/...`
        - `StockZHAMarketCategorySWHY`: source unified to `amazing_data`, taxonomy=`swhy`; `code` now uses `INDUSTRY_CODE` (not `INDEX_CODE`); `index_code` stored as separate field; parent_code logic fixed to use INDUSTRY_CODE hierarchy
        - `StockZHAIndustryConstituentSWHY`: extracts pure `symbol` from `con_code` (e.g. `603648.SH` → `603648`); adds taxonomy/market to sink
        - `StockZHAIndustryWeightSWHY`: same symbol extraction + taxonomy/market
        - `StockZHAIndustryDailySWHY`: adds taxonomy/market to sink
        - `StockZHAMarketCategory` (Mairui): adds taxonomy=`mairui`, market=`zh_a` to sink
- v0.23.1
    - Fixed bugs of downloading industry/company actions/financial stuff.
- v0.23.0
    - **Field Catalog & Data Documentation**
        - New `artemis/consts/field_catalog.py` — single source of truth for all JSON-stored data field metadata
        - Structured as `CATALOG[domain][data_type] → List[FieldEntry]` for extensibility
        - Covers all 7 data types: balance_sheet, income, cashflow, profit_express, profit_notice, dividend, right_issue
        - Public API: `get_fields()`, `get_field_names()`, `get_field_map()`, `list_domains()`, `list_types()`
        - New `docs/financial_data_fields.md` — comprehensive field reference documentation with query examples
- v0.22.0
    - **Balance Sheet financial data pipeline** (`get_balance_sheet`)
        - New task: `STOCK_ZH_A_BALANCE_SHEET` — downloads A-share balance sheet data via AmazingData SDK
        - Design: uses JSON column for 180+ financial fields (extensible to income/cashflow statements)
        - PhoenixA client: `upsert_financial_statements()`, `query_financial_statements()`
        - Batch sink in 500-record chunks to avoid oversized HTTP requests
    - **Cash Flow financial data pipeline** (`get_cash_flow`)
        - New task: `STOCK_ZH_A_CASH_FLOW` — downloads A-share cash flow statement data via AmazingData SDK
        - Reuses unified `financial_statement` table with `statement_type='cashflow'`
    - **Income Statement financial data pipeline** (`get_income`)
        - New task: `STOCK_ZH_A_INCOME` — downloads A-share income statement data via AmazingData SDK
        - Reuses unified `financial_statement` table with `statement_type='income'`
    - **Profit Express financial data pipeline** (`get_profit_express`)
        - New task: `STOCK_ZH_A_PROFIT_EXPRESS` — downloads A-share profit express data via AmazingData SDK
        - Reuses unified `financial_statement` table with `statement_type='profit_express'`
        - Handles both DataFrame and dict return types from SDK
    - **Profit Notice financial data pipeline** (`get_profit_notice`)
        - New task: `STOCK_ZH_A_PROFIT_NOTICE` — downloads A-share profit notice (业绩预告) data via AmazingData SDK
        - Reuses unified `financial_statement` table with `statement_type='profit_notice'`
        - Handles both DataFrame and dict return types from SDK
    - **Financial reference data constants** (`artemis/consts/financial.py`)
        - `FinancialStatementType`: balance_sheet / income / cashflow / profit_express / profit_notice
        - `ReportType`: Q1(1) / H1(2) / Q3(3) / Annual(4)
        - `StatementCode`: 1-10 most common codes (合并报表/母公司报表/调整/更正前 etc.)
        - `ProfitNoticeType`: 1-11 业绩预告类型代码 (略减/略增/扭亏/预减/预增 etc.)
    - **Corporate Action data pipeline** — new domain: `corporate_action` table
        - New task: `STOCK_ZH_A_DIVIDEND` — downloads A-share dividend data via AmazingData SDK (`get_dividend`)
        - New task: `STOCK_ZH_A_RIGHT_ISSUE` — downloads A-share right issue data via AmazingData SDK (`get_right_issue`)
        - New `BaseCorporateActionTask` base class with configurable metadata fields
        - PhoenixA client: `upsert_corporate_actions()`, `query_corporate_actions()`
        - Reference constants: `CorporateActionType`, `DividendProgress`, `RightIssueProgress`
    - Added to `tasks_export_2026-04-26.json`:
        - `stock_zh_a_balance_sheet`: weekly SAT 06:00, ASYNC mode with 30min timeout
        - `stock_zh_a_cash_flow`: weekly SAT 06:30, ASYNC mode with 30min timeout
        - `stock_zh_a_income`: weekly SAT 07:00, ASYNC mode with 30min timeout
        - `stock_zh_a_profit_express`: weekly SAT 07:30, ASYNC mode with 30min timeout
        - `stock_zh_a_profit_notice`: weekly SAT 08:00, ASYNC mode with 30min timeout
        - `stock_zh_a_dividend`: weekly SAT 08:30, ASYNC mode with 30min timeout
        - `stock_zh_a_right_issue`: weekly SAT 09:00, ASYNC mode with 30min timeout
- v0.21.0
    - **Renamed diagnostics → bar_details / Transaction Details**
        - `enable_diagnostics` → `enable_bar_details`, `diagnostics_level` → `bar_details_level` across base strategy, executor, workbench model, and result normalizer
        - `diagnostic_events` → `bar_detail_events` in strategy instance
        - Artifacts key `diagnostics` → `bar_details`
    - **Fixed `_getpairs()` ValueError in `@register_strategy` decorator**
        - Changed to `_getitems()` which returns proper `(key, value)` tuples instead of bare strings
    - **Added order result diagnostics in base strategy**
        - `notify_order()` now records `ORDER_BUY_OK` / `ORDER_SELL_OK` / `ORDER_FAILED` events to bar_detail_events
        - Shows execution price, size, commission for completed orders; reason and cash for failed orders
    - **Grid trading: pre-order cash check**
        - Before placing BUY, checks `estimated_cost > available_cash` and records SKIP with reason "资金不足" instead of letting backtrader margin-reject
    - **FIFO order-pair trade statistics fallback**
        - When backtrader TradeAnalyzer reports 0 trades (position never returns to 0, e.g. grid trading), uses FIFO matching of completed BUY/SELL orders to compute trade_count, win_count, loss_count, win_rate
    - **Industry Daily data pipeline** (`get_industry_daily`)
        - New task: `STOCK_ZH_A_INDUSTRY_DAILY_SWHY` — downloads SWHY industry index daily OHLCV + PE/PB/market cap
        - PhoenixA client: `upsert_industry_daily()`, `query_industry_daily()`
        - PhoenixA client: `query_industry_categories()`, `query_industry_constituents_by_index()`, `query_industry_constituents_by_stock()`
    - **Workbench Industry Explorer API**
        - `GET /workbench/industry/categories` — industry hierarchy query
        - `GET /workbench/industry/constituents` — constituents by industry index
        - `GET /workbench/industry/by-stock` — reverse lookup: stock → industries
        - `GET /workbench/industry/daily` — industry index daily bars
    - Added to `tasks_export_2026-04-25.json` (daily at 17:30 MON-FRI)
- v0.20.1
    - Upgraded baostock to v0.9.1
    - Added record of a grid line of an order in grid line trading.
    - Added grid rollback in grid trading.
- v0.20.0
    - **Bugfix: STOCK_ZH_A_HIST field name mismatch**
        - Fixed `date→trade_date`, `code→symbol` field mapping for PhoenixA v2 API compatibility
        - Separated standard bars and baostock ext data (`turn`, `pe_ttm`, etc.) into `bars` + `ext` payloads
        - Renamed `pctChg→pct_chg` to match PhoenixA `StandardBar` JSON tags
        - Fixed empty `trade_date` causing MySQL `Error 1292` on upsert
    - **Unified `code→symbol` variable naming across codebase**
        - `stock_zh_a_hist_child.py`: `code→bs_code`, `raw_code→symbol`; sink calls `upsert_bars()` (v2) instead of `upsert_stock_zh_a_hist()` (legacy)
        - `stock_zh_a_hist_parent.py`: `code_list→symbol_list`, `codes→symbols`, `code_infos→symbol_infos`; calls `get_securities()` / `get_bars_last_update()` (v2)
        - `campaign.py`: `code_infos→symbol_infos`; migrated to v2 API
        - `phoenixA_client.py`: removed `"code"` backward-compat field from `get_securities()` return
        - `config/task.yaml`: removed `code`, `adjustflag`, `tradestatus`, `isST` from baostock fields
        - Updated all tests and fake clients to match v2 API signatures
    - **Added return rate curve (收益率曲线)**
        - `result_normalizer.py`: computes `return_curve` from `equity_curve` + `start_cash`, outputs `return_pct` per bar
        - Added `return_curve` to `artifacts`, `plot_series`, and `plot_manifest`
        - Frontend: new `ReturnCurveChartComponent` with ECharts percentage Y-axis
    - **Added Grid Trading strategy (网格策略)**
        - New `strategies/grid_trading.py`: `@register_strategy(code="grid_trading")`
        - Params: `upper_price`, `lower_price`, `grid_lines`, `order_size`, `grid_mode` with full `param_schema`
        - `grid_mode`: `arithmetic` (等差/固定价差) or `geometric` (等比/log 等差)
        - Each grid line independently tracks position state (filled/empty)
        - Auto-discovered by frontend via `GET /workbench/strategies`
    - **Added K-Line chart with Buy/Sell markers in backtest results**
        - Backend: includes original OHLCV bars in backtest response `artifacts.bars`
        - Frontend: new `BacktestKLineChartComponent` — candlestick chart with green ▲"B" / red ▼"S" signal annotations
        - Placed above equity curve chart in research page layout
    - **Added strategy engine documentation**
        - `docs/2026-04-15 STRATEGY_ENGINE_STRATEGIES_GUIDE.md`: comprehensive guide covering architecture, base class, registration, parameter schema, each strategy's logic/params/code, data flow, frontend integration, and how to add new strategies
    - Added `test_stock_hist_symbol_v2.py`: 22 tests for code→symbol refactoring
    - Updated `test_workbench_providers.py`: migrated fake clients to v2 API
- v0.19.0
    - Use previous python env to build image when there is no dependency update.
- v0.18.0
    - **Strategy Engine DX Refactor — BaseRecordingStrategy + Decorator Registration + Shared Executor**
    - New `strategies/base.py`:
        - `BaseRecordingStrategy` base class: unifies equity_curve / position_curve / signal_events / order_events / trade_events recording across all strategies; subclasses only implement `__init__()` + `on_bar()`
        - `@register_strategy` decorator: auto-extracts `default_params` from backtrader `params` via `_getpairs()`, eliminates duplicate definition between strategy class and registry_map
    - New `strategy_engine/executor.py`:
        - `execute_backtest()` — shared backtest execution function used by both Workbench and TaskEngine paths
        - `extract_analyzer_results()` — single implementation, replaces duplicates in `workbench/backtest.py` and `task_engine/backtest/run.py`
    - Enhanced `StrategySpec.validate_params()`:
        - Added `float` type + min/max range validation
        - Added `str` type validation
        - Added `enum` type + options validation
        - Added `max` upper bound for int/float
        - Added `default`, `description`, `display_name` metadata fields for frontend form rendering
    - Added `version` field to `StrategySpec` (default `"v1"`)
    - Refactored `SmaCrossStrategy`: 111 lines → 46 lines (with decorator metadata), pure strategy logic ~20 lines
    - Refactored `services/workbench/backtest.py`: uses `execute_backtest()`, removed duplicate `_extract_analyzer_results`
    - Refactored `task_engine/backtest/run.py`: uses `execute_backtest()`, removed duplicate `_extract_analyzer_results`
    - `strategy_registry.py` now supports both decorator-based and legacy `registry_map.py` registration (backward compatible)
    - Test fixes:
        - Fixed `test_backtrader_phase1.py`: `timeframe` → `period` in test request bodies and assertions (aligned with v0.17.0 rename)
        - Fixed `test_workbench_phase1_contract.py`: `FakePhoenixClient` updated from `get_stock_zh_a_hist_bars()` to `get_bars()` (aligned with v0.17.0 provider refactor); monkeypatch adapted for `execute_backtest` path
    - Added architecture review doc: `docs/2026-04-15 REVIEW_STRATEGY_ENGINE_ARCHITECTURE.md`
- v0.17.0
    - **Unified field naming & PhoenixA v2 API migration**
    - PhoenixAClient refactored to use v2 API endpoints:
        - `get_bars()` / `upsert_bars()` — unified bars via `/api/v2/bars/{asset_type}/{market}`
        - `get_securities()` / `upsert_securities()` — via `/api/v2/securities`
        - `upsert_taxonomy_categories()` — via `/api/v2/taxonomy/{source}/categories/upsert`
        - Automatic `trade_date→date` / `symbol→code` field mapping for CacheEngine compatibility
    - All legacy client methods preserved as backward-compatible aliases
    - Replaced per-asset-type providers with unified `PhoenixBarsProvider`
    - Renamed `timeframe` → `period` across all backtest code:
        - `BacktestResultNormalizer`, `BacktraderRunTask`, `BacktraderCampaignTask`
        - Removed `timeframe→period` remapping hack in `workbench/backtest.py`
    - Updated `DataProviderRegistry`:
        - New primary spec: `phoenixa_bars` (supports daily/weekly/min5/min15/min30/min60)
        - Legacy alias `phoenixa_hist_daily` preserved for existing task configs
- v0.16.0
    - Refactor backend query contracts in workbench.py and workbench_routes.py to standardize period, current, and source validation.
    - Update source scanning in config_manager.py so available_sources() returns relx/home/production and current, never default.
    - Centralize normalization in market_data.py and backtest.py via a shared normalize_dimensions() and MarketDataQuery-style flow.
    - Update existing tests in test_config_manager.py and add route/service/frontend coverage for period, current, relx, and index -> nf behavior.
    - Added Provider Layer。
- v0.15.1
    - Added SWHY stock category
- v0.15.0
    - Added phase I of CacheEngine.
        - Added Arrow + mmap
        - Added compaction API
        - Added Partition Resolver
        - Added unit tests for cache engine
        - Added multiple data dimension in workbench
- v0.14.0
    - Added Task Cancel API, invoked by cronjob.
    - Added Timeout control for task engine.
    - Added exchange support for filtering stock list.
    - Fixed issue of using wrong parameters in STOCK_ZH_A_HIST.
- v0.13.1
    - Added exchange filter in STOCK_ZH_A_LIST
- v0.13.0
    - Added multi-datasource in Workbench and related data provider interfaces.
- v0.12.2
    - Added design documents and proposals.
    - Refactor to extract services.
- v0.12.1
    - Added skewness, kurtosis, support and breakout indicators.
- v0.12.0
    - Extracted `analyzers/` and `data_providers/` into independent module folders under `strategy_engine`, aligning with the existing `strategies/` structure.
    - Added Strategy Research Workbench.
- v0.11.1
    - Fixed design flaws of strategy engine.
    - Rename backtrade units to bt_engine.
    - Added comments to core logic.
- v0.11.0
    - Added BACKTRADER_CAMPAIGN and BACKTRADER_RUN.
    - Added strategies engine to work with task framework.
    - Added strategies API in phoenixA_client.py
    - Changed docs of artemis.
- v0.10.4
    - Commented SDKs auto disconnection.
    - Fixed wrong callback parameters to cronjob.
    - Updated deployment script and docker files.
    - Fixed wrong parameters in StockZhAHistChild.
- v0.10.3
    - Cronjob added dropbox to input HTTP Method.
    - Added new development env.
- v0.10.2
    - Use AmazingData to download stock_zh_a_list.
    - Updated deployment process.
- v0.10.1
    - Updated deployment scripts to support auto proxy.
- v0.10.0
    - Eliminated RAISE abuse in concrete task units and replaced with proper exception handling and error reporting.
    - Take off redundant try-catch structure from concreted tasks.
- v0.9.4
    - Rename parent and child to orchestrator and worker.
- v0.9.3
    - Semanticized parameters of stock_zh_a_hist.
    - Given new order of base task unit.
- v0.9.2
    - Added mapping from stock to market category
    - Refactored stoch_zh_a_hist task unit to support phoenixA change.
- v0.9.1
    - Added stock_zh_a_hist task unit to fetch historical data of stock zh A.
    - Fixed using wrong task code issue in parent task.
    - Added utils to safe convert data type.
    - Added logs.
- v0.9.0
    - Added sdk manager to manage multiple sdks, baostock sdk & amazingData is supported.
- v0.8.0
    - Added task dynamic control of task creation, registry, update and deletion.
- v0.7.0
    - Added runtime update of task units.
    - Added runtime update of task parameters.
    - Added runtime registry of task units.
- v0.6.1
    - Added missing requirements in requirements.txt
- v0.6.0
    - Refactored task unit design.
- v0.5.1
    - Extended task list to provide much details.
- v0.5.0
    - Dropped callback in context and use dept_clients as substitution.
    - Added phoenixA client to sink data.
- v0.4.1
    - Move task_code to task_run url and adjusted related parameter validation.
    - Fixed incompatible issue of parent task.
- v0.4.0
    - Simplified Task validation and optimized construction of task execution.
    - Modelled data structures to substituted dict for better type hinting and code completion.
    - Migrated callback to core
    - Added consts folder to support global constants definition.
- v0.3.3
    - Optimized logger output: Removed traceid & spanid from message field.
    - Added generation of traceid/spanid in logger when missing, this also compatible with go framework.
    - Remove useless TraceLogger.
- v0.3.2
    - Optimized router api logic and removed useless code.
    - Optimized logger: removed event_type in logger base.
    - Fixed caller to use real project path rather than os path.
    - Removed task_meta from models
- v0.3.1
    - Added control of OTEL
    - Added middleware to process traceid
    - Changed some logs of task framework
- v0.3.0
    - Added stock list fetch for stock ZH
- v0.2.2
    - Added auto-deploy scripts.
    - Added docker file and docker compose file.
- v0.2.1
    - Added stub for stats and enable stats in response.
    - Added stock_zh_a_hist as sample task in examples folder.
    - Added _report_progress in parent task.
    - Added task check in api route.
    - Fixed bug of unable passing through exception to http response.
- v0.2.0
    - Re-designed the task engine and task workflow.
    - Simple task run and hierarchical task run supported.
    - Task parameterized execution supported.
    - Removed unused sources & sink packages.
    - Optimized BaseTaskUnit logic and make log more clear.
    - Extracted task status to ENUM and replaced magic numbers with ENUM.
- v0.1.3
    - Added global callback to notify cronjob results & progress.
    - Added overwrite config for cronjob ip/port.
    - Made Artemis and cronjob compatible with each other.
    - Added SYNC/ASYNC mode for task execution.
- v0.1.2
    - Optimized traceid/span using in basic components.
    - Removed redundant otel code from business logic.
- v0.1.1
    - Fixed logs config and generation issues.
    - Refactored code of otel.
    - Added requirements.txt
    - Added task example
- v0.1.0
    - Initialized project with basic structure and README file.