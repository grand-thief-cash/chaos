# VERSION
v0.10.0

# Changelog
- v0.10.0
  - Added http client component.
- v0.9.0
  - Added GORM component.
  - Replaced some const value in grpc client component
- v0.8.1
  - GRPC server/client component switched to use open telemetry.
- v0.8.0
  - Added open telemetry component.
  - Modified http server component to use open telemetry and removed manual traceid handling.
  - Added goimports setup scripts in scripts folder.
- v0.7.0
  - Added prometheus metrics component.
  - Added documents for prometheus metrics component.
- v0.6.5
  - Adjusted traceid key to "trace-id" and added it in consts file.
  - Adjusted the tidy_go.bat script.
- v0.6.4
  - Added the go mod tidy script in scripts folder.
  - Adjusted the order of imports in some files.
- v0.6.3
  - Refined grpc client component, added "ConnectOnStart" option to control whether to connect when starting the component.
  - Verified grpc client component in poc client.
  - Versified grpc server component in poc application.
  - Added Unusable go mod scripts in scripts folder.
- v0.6.2
  - poc/application: Added mysql, redis dependencies[Not tested].
- v0.6.1
  - Tested GRPC server component.
  - In application poc, craeted echo service and registered to GRPC server.
  - Added a client in poc to test GRPC server and it works.
  - Adjusted order of imports in some files.
  - Added replace in infra/common/go.mod to make local import work.
- v0.6.0
  - Added GRPC server component.
  - Refactored poc structure to makes it more consistent with the structure of the infra.
  - Changed the structure of "common" in infrastructure, but this may be changed later...
- v0.5.0
  - Added Redis component.
- v0.4.1
  - Added customized dependencies in each component.
  - Replaced component name using consts.
  - Simplified register process handler func to http server
  - Removed "Register http handler func after run", this violates the whole booting process.
- v0.4.0
  - Added http server component (Init version and non-testable).
  - RunWithContext() blocks until the provided context is done and on context cancellation it triggers graceful shutdown.
- v0.3.0
  - Fixed logging callerSkip, not the caller in log is correct.
  - Added Mysql components
- v0.2.2
  - Replaced go log before init logging component.
- v0.2.1
  - Added logging component to support global logging across the application(Except core).
- v0.2.0
  - A basic version of application module has beed added.
  - Added initial setup for application module.
  - Available commands: start, stop
  - Integrated logging and configuration management.
- v0.1.2[Unfinished]
  - Refactored codebase for improved performance and readability.
- v0.1.1
  - Reconstructed the infra_go module for better modularity and maintainability.
  - update go.mod and go.sum to include latest dependencies.
- v0.1.0
  - Initial release of the infra_go module.
  - Added basic infrastructure setup for Go applications.
  - Included example configurations for common use cases.
  - Implemented logging and error handling utilities.
  - Added poc_go for testing infra_go functionalities.
